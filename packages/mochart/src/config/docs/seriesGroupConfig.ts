export default function getDescriptions() {
  return {
    id: 'the unique identifier for the series group so it can be referenced by series that belong to it',
    ignore: 'whether to ignore this series group and treat it as though it were not specified',
  };
}