---
"@mochart/core": minor
---

add categoryAxis.ticks, explicit { value, label?, minor? } ticks that replace the generated ones so a daily ordinal date axis can label only chosen categories such as every Monday; on a keyProperty axis a tick's value is matched by category key, given as the key's string or number form
