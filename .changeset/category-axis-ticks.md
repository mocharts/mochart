---
"@mochart/core": minor
---

add categoryAxis.ticks, explicit { value, label? } ticks that replace the generated ones so a daily ordinal date axis can label only chosen categories such as every Monday
