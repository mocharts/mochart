---
"@mochart/core": minor
---

add a font config (family, size as pixels or any css font-size string, weight, style) to every text part and a chart-wide chart.font default, written as an inline style on each text element so a configured value wins over host page css while a null member stays with css
