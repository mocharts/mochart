---
"@mochart/core": minor
---

add categoryAxis.categoryValueInterval, the axis value distance one category slot covers on a linear scale: "auto" (the default) for the smallest gap between neighbouring categories, a number in axis values, or on a date axis a millisecond count or one of second, minute, hour, day, week; an ordinal axis keeps "auto"
