# Changesets

Pending release notes live here as `*.md` files created by `npx changeset`.
Merging the "Version Packages" PR consumes them into the package changelogs
and publishes. See [RELEASE.md](../RELEASE.md) for the full process.
