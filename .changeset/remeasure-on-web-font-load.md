---
"@mochart/core": patch
---

measure the chart's text again, and refit truncated titles, legend items, tick labels and axis titles, when the document reports a web font finishing loading, instead of keeping the fallback font's measurements until the next config change or resize
