.DS_Store
node_modules
coverage
.idea
.vscode
dist
generated
**/.vitepress/cache
/site
.svelte-kit
test-results
playwright-report
# Screenshot reference sets for the demo layout gate (megabytes of PNGs, and
# machine-specific: font rasterization differs between platforms, so a set
# captured here will not match one captured elsewhere). The harness itself —
# scripts/screenshots/{capture,compare}.mjs — is tracked; only the images are not.
scripts/screenshots/refs
# Build artifact, regenerated by every install/build (editor prebuild); never stale because never committed.
packages/mochart-editor/src/mochartConfigModel.generated.ts
# Release dry-run tarballs (scripts/pack-libs.mjs)
/pack
# Config fuzzer output (test/fuzz) — a findings report regenerated by every run.
/packages/mochart/.fuzz
